stages:
  - deploy

publish:
  stage: deploy
  image: node:10-alpine
  script:
    - echo '//registry.npmjs.org/:_authToken=${NPM_TOKEN}'>.npmrc
    - npm install
    - npm publish --access public
  when: manual
